sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop  
    loaded_at_field: _etl_loaded_at # changed to config in v1.10

    tables:
      - name: customers
      - name: orders
        freshness:
          warn_after:
            count: 12
            period: hour
          error_after:
            count: 20
            period: hour
        loaded_at_field: _etl_loaded_at