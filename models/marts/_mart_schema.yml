models:
  - name: fct_orders
    group: finance
    access: protected
    columns:
      - name: amount_usd
        data_tests:
        - assert_positive_values:
            config:
              severity: warn

      - name: order_id
        data_tests:
          - not_null_or_empty_string
          - dbt_utils.not_empty_string